1. **Functional components only** — no new class components.
   <!-- source: stack(react) -->
   **Status**: enforced

2. **Tests live next to the component they test**, not in a parallel mirrored tree.
   <!-- source: stack(react) -->
   **Status**: adopting
