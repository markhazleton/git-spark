1. **TypeScript, strict mode.** No new `.js` source files; `any` needs a stated reason, not a shortcut.
   <!-- source: stack(node) -->
   **Status**: enforced

2. **ESM modules only** — no new CommonJS `require`/`module.exports`.
   <!-- source: stack(node) -->
   **Status**: enforced
