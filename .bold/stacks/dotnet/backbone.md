1. **Nullable reference types are enabled** — no project opts back into implicit non-null.
   <!-- source: stack(dotnet) -->
   **Status**: enforced

2. **Minimal APIs by default** — a controller-based MVC endpoint needs a stated reason, not just habit.
   <!-- source: stack(dotnet) -->
   **Status**: adopting
