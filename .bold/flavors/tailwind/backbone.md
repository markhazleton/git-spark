1. **Utility classes, not inline `style=` attributes or new bespoke CSS files** — a genuinely reusable pattern becomes a component, not a new class.
   <!-- source: flavor(tailwind) -->
   **Status**: enforced
