1. **Reach for a Bootstrap component before writing custom CSS** — a custom override needs a stated reason (Bootstrap genuinely can't do it), not just preference.
   <!-- source: flavor(bootstrap) -->
   **Status**: enforced
